// Twitter uses the same image as Open Graph. Re-exporting keeps it
// in lockstep with /opengraph-image so we maintain one source.
export { default, alt, size, contentType } from "./opengraph-image";
